import { SignInPage } from "@/features/(private)/auth/sign-in"

export default function Page() {
  return (
    <div className="flex h-screen items-center justify-center">
      <SignInPage />
    </div>
  )
}
